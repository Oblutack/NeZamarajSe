<%# Decorative only - sits behind a page's header block. The parent
    container needs `relative overflow-hidden` for this to clip correctly. %>
<div class="absolute inset-x-0 top-0 h-28 dot-pattern-band pointer-events-none" aria-hidden="true"></div>
