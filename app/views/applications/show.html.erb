<!-- app/views/applications/show.html.erb -->
<div class="max-w-3xl mx-auto py-8">
  <%= link_to "&larr; #{t(".back_to_crm")}".html_safe, crm_path, class: "text-[12.5px] font-semibold text-zinc-500 hover:text-zinc-900 dark:text-zinc-400 dark:hover:text-zinc-100" %>

  <div class="mt-3 mb-6 flex items-start justify-between gap-4">
    <div>
      <h1 class="text-[22px] font-bold text-zinc-950 tracking-tight dark:text-zinc-50"><%= @application.job.title %></h1>
      <p class="mt-1.5 text-[13px] text-zinc-500 dark:text-zinc-400">
        <%= link_to @application.job.company.name, company_path(@application.job.company), class: "hover:underline" %>
        &middot; <span class="font-semibold text-zinc-700 dark:text-zinc-300"><%= application_status_label(@application.status) %></span>
      </p>
    </div>

    <% if @application.applied? %>
      <%= link_to t(".send_follow_up"), compose_follow_up_application_path(@application), class: "flex-shrink-0 inline-flex items-center px-4 py-2.5 text-[13px] font-semibold rounded-md text-white bg-zinc-900 hover:bg-zinc-800 active:scale-[0.98] dark:text-zinc-900 dark:bg-white dark:hover:bg-zinc-200" %>
    <% end %>
  </div>

  <% if @application.needs_follow_up? %>
    <div class="mb-6 border border-amber-200 bg-amber-50 text-amber-800 px-4 py-3 rounded-lg text-[13px] dark:border-amber-900 dark:bg-amber-950/40 dark:text-amber-400">
      <span class="font-semibold"><%= t(".no_reply_in_days", count: Rails.application.config.follow_up_after_days) %></span> <%= t(".worth_a_follow_up") %>
    </div>
  <% end %>

  <div class="bg-white border border-zinc-200 rounded-lg p-6 mb-6 dark:bg-zinc-900 dark:border-zinc-800">
    <h2 class="text-[13px] font-semibold text-zinc-700 mb-4 dark:text-zinc-300"><%= t(".details") %></h2>

    <%= form_with model: @application, url: application_path(@application), method: :patch, class: "grid grid-cols-1 sm:grid-cols-2 gap-4" do |f| %>
      <div>
        <label class="block text-[12px] font-semibold text-zinc-500 mb-1 dark:text-zinc-400"><%= t(".contact_person") %></label>
        <%= f.text_field :contact_person, placeholder: t(".contact_person_placeholder"), class: "block w-full rounded-md border-zinc-300 text-[13px] focus:border-zinc-500 focus:ring-1 focus:ring-zinc-500 p-2 border placeholder:text-zinc-400 dark:border-zinc-700 dark:focus:border-zinc-400 dark:focus:ring-zinc-400 dark:placeholder:text-zinc-600" %>
      </div>
      <div>
        <label class="block text-[12px] font-semibold text-zinc-500 mb-1 dark:text-zinc-400"><%= t(".salary") %></label>
        <%= f.text_field :salary, placeholder: t(".salary_placeholder"), class: "block w-full rounded-md border-zinc-300 text-[13px] focus:border-zinc-500 focus:ring-1 focus:ring-zinc-500 p-2 border placeholder:text-zinc-400 dark:border-zinc-700 dark:focus:border-zinc-400 dark:focus:ring-zinc-400 dark:placeholder:text-zinc-600" %>
      </div>
      <div>
        <label class="block text-[12px] font-semibold text-zinc-500 mb-1 dark:text-zinc-400"><%= t(".interview_date") %></label>
        <%= f.datetime_field :interview_date, class: "block w-full rounded-md border-zinc-300 text-[13px] focus:border-zinc-500 focus:ring-1 focus:ring-zinc-500 p-2 border dark:border-zinc-700 dark:focus:border-zinc-400 dark:focus:ring-zinc-400" %>
      </div>
      <div>
        <label class="block text-[12px] font-semibold text-zinc-500 mb-1 dark:text-zinc-400"><%= t(".rejection_reason") %></label>
        <%= f.text_field :rejection_reason, placeholder: t(".rejection_reason_placeholder"), class: "block w-full rounded-md border-zinc-300 text-[13px] focus:border-zinc-500 focus:ring-1 focus:ring-zinc-500 p-2 border placeholder:text-zinc-400 dark:border-zinc-700 dark:focus:border-zinc-400 dark:focus:ring-zinc-400 dark:placeholder:text-zinc-600" %>
      </div>
      <div class="sm:col-span-2 flex justify-end pt-2">
        <%= f.submit t(".save_details"), class: "cursor-pointer inline-flex items-center px-4 py-2 text-[13px] font-semibold rounded-md text-white bg-zinc-900 hover:bg-zinc-800 active:scale-[0.98] dark:text-zinc-900 dark:bg-white dark:hover:bg-zinc-200" %>
      </div>
    <% end %>
  </div>

  <div class="bg-white border border-zinc-200 rounded-lg p-6 dark:bg-zinc-900 dark:border-zinc-800">
    <h2 class="text-[13px] font-semibold text-zinc-700 mb-4 dark:text-zinc-300"><%= t(".timeline") %></h2>

    <%= form_with url: add_note_application_path(@application), method: :post, class: "flex gap-2 mb-5" do %>
      <%= text_field_tag :body, nil, placeholder: t(".add_a_note"), class: "flex-1 rounded-md border-zinc-300 text-[13px] focus:border-zinc-500 focus:ring-1 focus:ring-zinc-500 p-2 border placeholder:text-zinc-400 dark:border-zinc-700 dark:focus:border-zinc-400 dark:focus:ring-zinc-400 dark:placeholder:text-zinc-600" %>
      <button type="submit" class="cursor-pointer inline-flex items-center px-4 py-2 text-[13px] font-semibold rounded-md text-white bg-zinc-900 hover:bg-zinc-800 active:scale-[0.98] dark:text-zinc-900 dark:bg-white dark:hover:bg-zinc-200"><%= t(".add") %></button>
    <% end %>

    <% if @application.application_events.empty? %>
      <p class="text-[13px] text-zinc-400 dark:text-zinc-600"><%= t(".no_activity_yet") %></p>
    <% else %>
      <ul class="space-y-3">
        <% @application.application_events.each do |event| %>
          <li class="text-[13px] flex gap-3">
            <span class="flex-shrink-0 text-zinc-400 text-[11.5px] w-32 pt-0.5 dark:text-zinc-600"><%= l(event.created_at, format: "%b %d, %H:%M") %></span>
            <span class="<%= event.event_type == "reply_detected" ? "font-semibold text-emerald-700" : "text-zinc-700" %>">
              <%= application_event_description(event) %>
            </span>
          </li>
        <% end %>
      </ul>
    <% end %>
  </div>
</div>
